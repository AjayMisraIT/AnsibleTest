---
- name: check hostname of server
  hosts: Win
  become: true  
  
  tasks:

    - name: check hostname
      win_shell: whoami
      register: fqdn
    
    - name: Display the fqdn
      debug:
        var: fqdn
